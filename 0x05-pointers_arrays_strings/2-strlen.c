#include "main.h"
#include <string.h>

/**
  * _strlen - prints the string length
  * @s: string pointer
  * Return: Always o
  */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
